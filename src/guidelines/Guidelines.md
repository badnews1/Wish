# ЗАПРЕЩЕНО ВНОСИТЬ ЛЮБЫЕ ИЗМЕНЕНИЯ В ЭТОТ ФАЙЛ БЕЗ ПРЯМОГО ЗАПРОСА И РАЗРЕШЕНИЯ ПОЛЬЗОВАТЕЛЯ

# ЗАПРЕЩЕНО ТАКЖЕ СОЗДАВАТЬ ЛЮБУЮ ДОКУМЕНТАЦИЮ В ПАПКАХ ПРОЕКТА ЕСЛИ ОБ ЭТОМ НЕ ПРОСИЛ ПОЛЬЗОВАТЕЛЬ НАПРЯМУЮ

# 🐛 Debugging Protocol

При любой проблеме:
1. **READ** - прочитай ВСЕ связанные файлы полностью
2. **ANALYZE** - проанализируй структуру и найди корневую причину
3. **FIX** - исправь корневую причину, не костыли

❌ НЕ выдвигай гипотезы без полной картины
❌ НЕ предлагай workaround'ы вместо исправления корня проблемы

# 🧹 Код без пользователей = Код без legacy

**У приложения пока нет реальных пользователей** → код обратной совместимости ЗАПРЕЩЁН.

❌ **НЕ создавать:**
- Deprecated экспорты (например, `export { newName as oldName }`)
- Реэкспорты для обратной совместимости
- Миграции данных и версионирование схем
- Проверки на старый формат данных

✅ **Вместо этого:**
- Рефакторить смело и напрямую
- Обновлять импорты везде сразу
- Менять структуру данных без оглядки на старое

# 🔄 DRY Принцип

Любой дублирующийся код **ДОЛЖЕН быть отрефакторен**.

- Код 2+ раза → вынести в функцию/компонент/хук
- Общая UI-логика → `shared/lib/hooks/`
- Бизнес-логика сущности → `entities/{entity-name}/model/`
- Чистые утилиты → `shared/lib/utils/` или `entities/{entity-name}/lib/`

# 📏 Размер файлов

- Один файл = одна ответственность (Single Responsibility Principle)
- Компонент > 200-300 строк → разбить на подкомпоненты
- Сложную логику выносить в отдельные хуки

---

# FSD Architecture Guidelines

## 📊 Схема зависимостей слоёв

```
app → pages → widgets → features → entities → shared
```

**Правила импортов:**
- ✅ **Вертикальная изоляция:** слой может импортировать только из нижележащих слоёв
- ❌ **Горизонтальная изоляция:** слайсы внутри одного слоя НЕ могут импортировать друг друга
- ⚠️ **Исключение entities:** могут делать cross-imports (с явной документацией @x/)
- ⚠️ **Исключение shared:** может импортировать shadcn/ui из `/components/ui/`

**Примеры:**
```tsx
// ✅ ПРАВИЛЬНО - импорт вниз по иерархии
// pages/wishlist/ → widgets/Header, features/create-wishlist, entities/wishlist
// widgets/Header → entities/user, shared/ui
// features/share-wishlist → entities/wishlist, shared/lib
// entities/wishlist → shared/ui, shared/lib

// ❌ НЕПРАВИЛЬНО - импорт вверх или горизонтальный
// shared/ui → entities/wishlist ❌
// features/create-wishlist → features/share-wishlist ❌
// widgets/Header → widgets/Footer ❌
```

## 🎯 Entity vs Feature

**Entity (Сущность)** — **ЧТО это**:
- Описывает бизнес-сущность (данные, структуру)
- Содержит CRUD операции и базовую логику сущности
- Примеры: `entities/wishlist`, `entities/collection`, `entities/user`

**Feature (Фича)** — **ЧТО можно сделать**:
- Описывает действие/сценарий использования
- Содержит бизнес-логику действия, формы, валидацию
- **Может композировать несколько entities**
- Примеры: `features/create-wishlist`, `features/share-wishlist`, `features/add-product-to-wishlist`

**Критерий разделения:**
```
✅ entities/wishlist — сама сущность (типы, CRUD, карточка)
✅ entities/product — сама сущность (типы, CRUD, карточка)
✅ features/share-wishlist — действие "поделиться вишлистом"
✅ features/add-product-to-wishlist — действие (композирует entities/product + entities/wishlist)

❌ features/wishlist-card — это UI сущности → entities/wishlist/ui/
❌ entities/create-wishlist — это действие → features/create-wishlist
```

### Features композируют Entities

```tsx
// ✅ features/add-product-to-wishlist/
import { ProductCard } from '@/entities/product';
import { useWishlistActions } from '@/entities/wishlist';
// Фича композирует две сущности для реализации действия

// ✅ features/share-wishlist/
import { WishlistCard } from '@/entities/wishlist';
import { useCurrentUser } from '@/entities/user';
// useShareActions живёт внутри самой фичи share-wishlist
```

**Когда выносить в feature:**
- Действие затрагивает несколько entities
- Есть сложная бизнес-логика взаимодействия
- Нужна форма или UI для действия

## Widget vs Feature

| Критерий | Widget | Feature |
|----------|--------|---------|
| **Главная цель** | Композиция UI для отображения | Бизнес-действие/сценарий |
| **Сложность логики** | Простая композиция | Сложная бизнес-логика |
| **Формы** | Нет форм с валидацией | Содержит формы с валидацией |
| **API-взаимодействие** | Через готовые хуки из entities | Может иметь собственное API |
| **State management** | Локальное UI-состояние | Бизнес-состояние с логикой |

**🚨 Пора выносить в feature если:**
1. Появилась форма с валидацией
2. Логика > 50-100 строк
3. Бизнес-правила (не просто "пустое/не пустое")
4. Повторное использование в нескольких местах
5. Собственное API для действия
6. Сложные side effects

## 📦 Именование

| Что | Стиль | Примеры |
|-----|-------|---------|
| **Папки слайсов** | kebab-case | `create-wishlist/`, `wishlist-detail/` |
| **Компоненты** | PascalCase | `WishlistCard.tsx`, `BaseDrawer.tsx` |
| **Хуки** | camelCase + use | `useWishlists.ts`, `useDrawer.ts` |
| **Утилиты** | camelCase | `formatPrice.ts`, `cropImage.ts` |
| **Типы** | PascalCase | `type Wishlist`, `interface User` |
| **Константы** | UPPER_SNAKE_CASE | `MAX_FILE_SIZE`, `API_URL` |
| **Конфиги (переменные)** | camelCase | `menuItems`, `tabsConfig` |

### Shared UI vs components/ui

**components/ui/** — Базовые примитивы из shadcn/ui:
- Низкоуровневые UI-компоненты (Button, Input, Select, Dialog)
- **НЕ модифицируются** напрямую
- ⚠️ **Особенность Figma Make:** папка системная, не переносится в FSD

**shared/ui/** — Кастомные компоненты проекта:
- Обёртки над shadcn с проектной стилизацией
- Композитные компоненты (BaseDrawer, SelectButton, CardImage)
- ✅ **МОЖЕТ импортировать** из `/components/ui/`

```tsx
// ✅ Базовый примитив из shadcn
import { Button } from '@/components/ui/button';

// ✅ Кастомный компонент проекта
import { BaseDrawer } from '@/shared/ui'; // обёртка над shadcn Drawer
```

### Pages и Роутинг — Flat Routing

**Структура:**
```
/app/routing/
  ├── AppRouter.tsx          # Главный роутер
  ├── HomeRouter.tsx         # Роутер для табов главной
  ├── navigationReducer.ts   # Логика навигации
  └── useAppNavigation.ts    # Хук навигации

/pages/
  ├── home/                  # Обёртка с табами
  ├── home-feed/             # Подстраница (таб Feed)
  ├── home-popular/          # Подстраница (таб Popular)
  └── wishlist-detail/       # Страница деталей
```

**Flat Routing правила:**
```
✅ ПРАВИЛЬНО: /pages/home-feed/, /pages/home-popular/
❌ НЕПРАВИЛЬНО: /pages/home/feed/ (вложенность запрещена)
```

## 📁 Структура слоёв

```
/app/                    # Инициализация приложения, роутинг, провайдеры
  ├── routing/           # Роутеры и навигационные хуки
  ├── providers/         # React-провайдеры (контексты, темы)
  ├── store/             # Глобальные сторы (useThemeStore, useAuthStore)
  └── config/            # Конфигурация приложения
  
/pages/{page-name}/
  ├── ui/                # Компонент страницы
  ├── model/             # Типы страницы (опционально)
  ├── lib/               # Вспомогательные функции
  └── config/            # Настройки (только ключи i18n)
      
/widgets/{widget-name}/
  ├── ui/                # Компоненты виджета
  ├── model/             # Типы виджета (опционально)
  ├── lib/               # Действия виджета
  └── config/            # Конфигурация (только ключи i18n)
      
/features/{feature-name}/
  ├── ui/                # Компоненты фичи
  ├── model/             # Типы, состояние, схемы валидации
  ├── lib/               # Вспомогательные функции
  ├── config/            # Настройки (только ключи i18n)
  └── api/               # API запросы (опционально)
      
/entities/{entity-name}/
  ├── ui/                # Компоненты сущности (Cards)
  ├── model/             # Типы, бизнес-логика (useWishlistActions)
  ├── lib/               # Чистые функции (formatters, constants)
  ├── config/            # Константы сущности
  └── api/               # CRUD операции (useWishlists, useCreateWishlist)
      
/shared/
  ├── ui/                # UI-компоненты (BaseDrawer, SelectButton)
  ├── lib/               # Хуки, утилиты
  │   ├── hooks/         # Кастомные хуки
  │   ├── utils/         # Вспомогательные функции
  │   └── validators/    # Технические валидаторы
  ├── api/               # Базовые API-утилиты (axios instance)
  ├── model/             # Общие типы
  └── config/
      └── i18n/          # Централизованные переводы
          ├── en.json
          └── ru.json
      
/components/ui/          # shadcn/ui (вне FSD)
```

## 📂 Сегменты — когда что использовать

### ui/ — UI-компоненты
- **Только разметка и UI-логика**
- Локальные константы компонента могут оставаться внутри
- Выносить в config/lib: переиспользуемые константы, тексты для i18n

### model/ — Бизнес-логика и типы
- **Типы и интерфейсы** сущности
- **Хуки с бизнес-логикой** (useWishlistActions)
- **Бизнес-валидация** (validateWishlistName)
- **Схемы валидации форм** (zod/yup)
- React-зависимые хуки с бизнес-логикой
- ❌ **НЕ содержит data-fetching хуки** — они в api/

### lib/ — Инфраструктурные утилиты
- **Форматтеры для отображения** (formatPrice, formatDate)
- **Простые валидаторы** (isValidUrl, isEmail) — технические проверки
- Трансформеры данных, чистые функции
- **Статические константы** — enum'ы, магические числа

**Правило model/ vs lib/:**
- **model/** — бизнес-правила домена ИЛИ React hooks
- **lib/** — инфраструктурные утилиты без знания о бизнес-логике

**Примеры:**
```tsx
// ✅ entities/wishlist/lib/formatters.ts — форматирование
export const formatWishlistDate = (date: Date) => date.toLocaleDateString();

// ✅ entities/wishlist/lib/constants.ts — константы
export const MAX_WISHLIST_NAME_LENGTH = 50;
export enum WishlistSortOrder { DATE_DESC = 'date_desc', NAME = 'name' }

// ✅ entities/wishlist/model/validators.ts — БИЗНЕС-валидация
export const validateWishlistName = (name: string) => {
  if (name.length < 3) return "Минимум 3 символа";
  if (name.length > 50) return "Максимум 50 символов";
  return null;
};

// ✅ shared/lib/validators.ts — ТЕХНИЧЕСКИЕ проверки
export const isValidUrl = (url: string) => /^https?:\/\//.test(url);
export const isValidEmail = (email: string) => /^[^\s@]+@[^\s@]+\.[^\s@]+$/.test(email);
```

### Работа с формами (zod/yup)

- **shared/lib/validators/** — технические валидаторы (email, url, phone)
- **features/{feature}/model/schema.ts** — бизнес-схемы форм
- **entities/{entity}/model/validators.ts** — бизнес-валидация сущностей

### config/ — Динамическая конфигурация
- **Конфигурационные объекты** (tabs, menuItems)
- **Опции компонентов** — варианты выбора
- ⚠️ **НЕ хранить тексты** — выносить в `shared/config/i18n/`

```tsx
// ✅ widgets/bottom-menu/config/menu-items.ts
export const menuItems = [
  { id: 'home', icon: Home, label: 'home.title' }, // ключ для i18n
  { id: 'search', icon: Search, label: 'search.title' }
];
```

### api/ — API запросы

**shared/api/** — базовая инфраструктура:
- Axios instance и конфигурация
- Типы (ApiResponse<T>, PaginatedResponse<T>, ErrorResponse)
- Обработчики ошибок

**entities/{entity}/api/** — CRUD операции:
- Data-fetching хуки (useWishlists, useWishlistById)
- Mutation хуки (useCreateWishlist, useDeleteWishlist)
- Query keys

**features/{feature}/api/** — бизнес-операции:
- Сложные операции (parseProduct, exportToPDF)
- Интеграции с внешними сервисами

**Направление зависимостей:**
- ✅ model/ → api/ (model композирует api и добавляет бизнес-логику)
- ✅ api/ → model/ — только типы
- ❌ api/ → model/ — хуки и функции (циклическая зависимость)

## 🔗 Правила импортов

### Публичный API (через index.ts)

```tsx
// ✅ ПРАВИЛЬНО - через публичный API с @/ алиасами
import { Header } from '@/widgets/Header';
import { useWishlists } from '@/entities/wishlist';

// ❌ НЕПРАВИЛЬНО - прямой импорт
import { Header } from '@/widgets/Header/ui/Header';
import { useWishlists } from '@/entities/wishlist/model/useWishlists';
```

**Экспортируй минимум необходимого** через `index.ts` — только то, что используется снаружи.

### Segment index files — внутренняя организация

**Каждый сегмент может иметь свой index.ts** для удобства импортов внутри слайса:

```
entities/wishlist/
  ├── ui/
  │   ├── WishlistCard.tsx
  │   ├── WishlistGrid.tsx
  │   └── index.ts              # ✅ Экспорт всех UI-компонентов
  ├── model/
  │   ├── types.ts
  │   ├── useWishlistActions.ts
  │   └── index.ts              # ✅ Экспорт типов и хуков
  ├── api/
  │   ├── useWishlists.ts
  │   ├── useCreateWishlist.ts
  │   └── index.ts              # ✅ Экспорт всех API-хуков
  └── index.ts                  # Главный публичный API слайса
```

## 🎯 Назначение слоёв

### `/pages/` - Тонкие обёртки (только композиция)

**Pages должны быть "тонкими"** — минимум логики, только композиция:

```tsx
// ✅ ПРАВИЛЬНО - page только композирует
function WishlistPage() {
  return (
    <div>
      <Header />
      <WishlistListWidget />
      <BottomMenu />
    </div>
  );
}

// ❌ НЕПРАВИЛЬНО - бизнес-логика в page
function WishlistPage() {
  const [wishlists, setWishlists] = useState([]);
  
  useEffect(() => {
    fetch('/api/wishlists').then(res => res.json()).then(setWishlists);
  }, []); // ❌ Логика должна быть в widgets/features/entities
  
  return <div>{wishlists.map(...)}</div>;
}
```

**Pages могут:**
- ✅ Композировать widgets и features
- ✅ Передавать параметры роута вниз (id из URL)
- ✅ Определять layout страницы

**Pages НЕ должны:**
- ❌ Содержать бизнес-логику
- ❌ Делать прямые API-вызовы
- ❌ Управлять состоянием (кроме локального UI)
- ❌ Содержать сложные useEffect

### `/app/` - Инициализация

- Слой должен быть минимальным
- Все провайдеры в `app/providers/`
- ❌ НЕ размазывать провайдеры по features/entities

### `/widgets/` - Композитные UI-блоки

**Widgets могут:**
- ✅ Композировать features, entities, shared
- ✅ Содержать логику композиции (как собрать несколько сущностей)
- ✅ Использовать хуки из entities/features

**Widgets НЕ должны:**
- ❌ Содержать бизнес-логику сущностей (CRUD, валидация)
- ❌ Содержать прямые API-вызовы
- ❌ Управлять глобальным состоянием

```tsx
// ✅ ПРАВИЛЬНО - widget использует хук из entities
import { useWishlists } from '@/entities/wishlist';

function WishlistWidget() {
  const { data: wishlists } = useWishlists();
  return <div>{wishlists.map(...)}</div>;
}

// ❌ НЕПРАВИЛЬНО - прямой API-вызов
function WishlistWidget() {
  useEffect(() => {
    axios.get('/api/wishlists').then(setWishlists); // ❌
  }, []);
}
```

## 🚫 Запрещено

1. **Переиспользуемые константы/тексты внутри UI** — выносить в config/lib
2. **Типы внутри UI-компонентов** — выносить в model/types.ts
3. **Импорт вверх по иерархии** (shared не может из entities)
4. **Бизнес-логика в shared/** — только "тупые" утилиты
5. **Горизонтальный импорт** между слайсами одного слоя
6. **Прямые импорты минуя index.ts**
7. **Вложенные подстраницы** `/pages/home/feed/` → `/pages/home-feed/`

## 🔄 Cross-imports между entities

⚠️ **Cross-imports — это КОМПРОМИСС**. Используйте только когда другие подходы не подходят.

**Приоритет:**
1. **Слот-паттерн** — РЕКОМЕНДУЕТСЯ
2. **Вынос в features** — если нужна бизнес-логика
3. **Cross-import** — крайний случай

### 1️⃣ Слот-паттерн
```tsx
function WishlistItem({ productSlot }: { productSlot: React.ReactNode }) {
  return <div>{productSlot}</div>;
}
// Использование
<WishlistItem productSlot={<ProductCard product={item.product} />} />
```

### 2️⃣ Вынос в features
```tsx
// features/wishlist-with-products/
import { WishlistCard } from '@/entities/wishlist';
import { ProductCard } from '@/entities/product';
```

### 3️⃣ Cross-import с папкой @x/
```
entities/wishlist/
  @x/
    product.ts  # Явный cross-import
```

```tsx
// entities/wishlist/@x/product.ts
export { ProductCard } from '@/entities/product';
export type { Product } from '@/entities/product';

// entities/wishlist/ui/WishlistItem.tsx
import { ProductCard } from '../@x/product';
```

| Ситуация | Подход |
|----------|--------|
| Простое отображение | **Слот-паттерн** ✅ |
| Сложная бизнес-логика | **Features** ✅ |
| Жёсткая неразрывная связь | **@x cross-import** ⚠️ |

## 🗄️ State Management

| Тип состояния | Где размещать | Инструмент |
|---------------|---------------|------------|
| Локальное UI (модалки, формы) | Внутри компонента | `useState` |
| Серверные данные (GET, POST) | `entities/{entity}/api/` | React Query |
| Глобальное клиентское (theme, auth) | `app/store/` | Zustand |
| Бизнес-логика сущности | `entities/{entity}/model/` | Хуки |
| Стор сущности (опционально) | `entities/{entity}/model/store.ts` | Zustand |

**Правила:**
- ❌ НЕ смешивать серверное состояние (React Query) с глобальным стором (Zustand)

## 🧪 Тестирование

**Размещение:** тесты рядом с кодом `{filename}.test.ts`

```
entities/wishlist/
  ├── ui/
  │   ├── WishlistCard.tsx
  │   └── WishlistCard.test.tsx
  ├── model/
  │   ├── useWishlistActions.ts
  │   └── useWishlistActions.test.ts
  └── lib/
      ├── formatters.ts
      └── formatters.test.ts
```

**Стратегия:**
- ✅ **Unit:** lib/, model/validators — обязательно
- ✅ **Integration:** внутри слайса (хук + API)
- ⚠️ **НЕ тестировать cross-layer** (pages → widgets → entities) — это E2E
- ✅ **E2E:** `/e2e/` вне FSD структуры

## 🌍 Интернационализация (i18n)

**Гибридный подход:** Распределённые переводы + общий словарь

**Структура:**
```
shared/config/i18n/          # ТОЛЬКО общие тексты (common, validation, errors)
entities/{entity}/config/i18n/   # Переводы сущности
features/{feature}/config/i18n/  # Переводы фичи
```

**Критерий размещения:**

| Категория | Где | Примеры |
|-----------|-----|---------|
| Общие действия | `shared/config/i18n/` | save, cancel, delete |
| Валидация/ошибки | `shared/config/i18n/` | required, email, network |
| UI сущности | `entities/{entity}/config/i18n/` | wishlist.title, product.price |
| Действия/формы | `features/{feature}/config/i18n/` | createWishlist.form.name |


## 🎨 Стилизация

- **Tailwind CSS v4**
- Цвет бренда: `#5F33E1` → `--color-accent`
- Шрифт: **Inter**
- Хардкоженные цвета → CSS-переменные из `/styles/globals.css`

## 📱 Особенности проекта

- Мобильное приложение (только портретная ориентация)
- Светлая тема с белым фоном
- Плавные скругленные углы
- Нижнее меню с центральной фиолетовой кнопкой
- Все комментарии в коде на русском языке