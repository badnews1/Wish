# 🔧 Как работать с миграциями в Figma Make

## ⚠️ Важно: Figma Make = веб-окружение

У вас **нет терминала**, поэтому Supabase CLI не подходит.  
Используйте **Supabase Dashboard** для выполнения SQL.

---

## ✅ Рекомендованный workflow:

### Когда нужно изменить БД:

#### 1️⃣ Скажите мне (AI) что нужно изменить

**Примеры:**
- "Добавь поле `theme` (текст) в таблицу `wishlists`"
- "Создай таблицу `wishlist_shares` для шаринга вишлистов"
- "Добавь индекс на поле `user_id` в `wishlists`"

#### 2️⃣ Я создам файл миграции

Создам файл `/supabase/migrations/YYYYMMDD_название.sql` с SQL кодом и обновлю типы в `/shared/api/database.types.ts`.

#### 3️⃣ Вы примените SQL в Dashboard

1. Откройте [Supabase Dashboard](https://supabase.com/dashboard/project/kcyupmixebkiewxplziz)
2. Перейдите в **SQL Editor**
3. Скопируйте SQL из файла миграции
4. Выполните (**Run**)

#### 4️⃣ Готово!

Типы уже обновлены, можно использовать в коде.

---

## 📋 Примеры частых миграций

### Добавить поле в существующую таблицу

```sql
ALTER TABLE wishlists ADD COLUMN theme text DEFAULT 'default';
```

### Изменить тип поля

```sql
ALTER TABLE wishlist_items ALTER COLUMN price TYPE numeric(12,2);
```

### Добавить индекс

```sql
CREATE INDEX idx_wishlists_user_id ON wishlists(user_id);
```

### Создать новую таблицу

```sql
CREATE TABLE wishlist_tags (
  id uuid PRIMARY KEY DEFAULT gen_random_uuid(),
  wishlist_id uuid REFERENCES wishlists(id) ON DELETE CASCADE,
  tag text NOT NULL,
  created_at timestamptz DEFAULT now()
);

ALTER TABLE wishlist_tags ENABLE ROW LEVEL SECURITY;

CREATE POLICY "Users can manage own tags" ON wishlist_tags
  FOR ALL USING (EXISTS (
    SELECT 1 FROM wishlists 
    WHERE id = wishlist_tags.wishlist_id 
    AND user_id = auth.uid()
  ));
```

---

## 🎯 Зачем хранить файлы миграций?

**Даже если выполняете SQL в Dashboard**, сохраняйте файлы потому что:

✅ **История изменений** - видно что и когда менялось  
✅ **Документация** - новый разработчик понимает структуру  
✅ **Откат** - можно посмотреть что было до  
✅ **Переезд** - легко воспроизвести БД на новом проекте

---

## 🚫 Что НЕ нужно делать:

❌ Устанавливать Supabase CLI (нет терминала)  
❌ Запускать bash скрипты (не работает в Figma Make)  
❌ Пытаться запустить `supabase db push`  

---

## 💡 Быстрая шпаргалка

| Что нужно | Что делать |
|-----------|------------|
| Добавить поле | Сказать AI → скопировать SQL → Dashboard |
| Создать таблицу | Сказать AI → скопировать SQL → Dashboard |
| Обновить типы | AI сделает автоматически |
| Откатить изменение | Сказать AI → напишет откат SQL |
| Посмотреть историю | Открыть `/supabase/migrations/` |

---

## 🎉 Итог

**Ваш workflow:**
1. Говорите мне что изменить в БД
2. Я создаю миграцию + обновляю типы
3. Вы выполняете SQL в Dashboard
4. Продолжаете разработку

**Просто и эффективно!** 🚀
